{
  "private": true,
  "license": "MIT",
  "author": "Viktor Nagy",
  "name": "express-server",
  "description": "Basic setup of an Express server with TypeScript",
  "version": "v1.0.0",
  "main": "index.ts",
  "devDependencies": {
    "@types/express": "^4.17.11",
    "@types/jest": "^26.0.20",
    "@types/node": "^14.14.31",
    "@types/nodemailer": "^6.4.0",
    "@types/redis": "^2.8.28",
    "@types/supertest": "^2.0.10",
    "@types/winston": "^2.4.4",
    "cross-env": "^7.0.3",
    "jest": "^26.6.3",
    "nodemon": "^2.0.7",
    "supertest": "^6.1.3",
    "ts-jest": "^26.5.3",
    "ts-node": "^9.1.1"
  },
  "dependencies": {
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "nodemailer": "^6.4.18",
    "redis": "^3.0.2",
    "typescript": "^4.2.2",
    "winston": "^3.3.3"
  },
  "scripts": {
    "git": "git add . && git commit -m ",
    "redis": "redis-server",
    "test": "cross-env NODE_ENV=test && jest --detectOpenHandles",
    "build": "rm -rf build && tsc --project .",
    "start": "yarn install && cross-env NODE_ENV=development nodemon --exec ts-node index.ts"
  }
}
